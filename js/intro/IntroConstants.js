// Copyright 2015, University of Colorado Boulder

/**
 * Constants used in the Intro screen of Energy Forms and Changes.
 * @author Jesse Greenberg
 */
define( function( require ) {
  'use strict';

  return {

    // constants used by burners
    BURNER_EDGE_TO_HEIGHT_RATIO: 0.2, // Multiplier empirically determined for best look.
    PERSPECTIVE_ANGLE: Math.PI / 4 // Positive is counterclockwise, a value of 0 produces a non-skewed rectangle.

  };
} );